# Tage VC Operating System — Phase 0

Greenfield Next.js rebuild lives in **`tagevc-os/`**. Spec SSOT: `docs/spec/Tage-VC-Operating-System.xlsx`.

The Vite app at repo root remains the live `portal.tagevc.com` until cutover.

```bash
cd tagevc-os && npm run dev
```

See `tagevc-os/README.md` for auth setup and module map.
