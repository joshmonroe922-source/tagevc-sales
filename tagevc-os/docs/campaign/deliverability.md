# Deliverability

- Per-entity sending domains + DNS wizard
- Suppressions O(1) pre-send
- RFC 8058 one-click
- Kill switch on `ecc_entity_flags`
- Postal when `POSTAL_API_URL` + `POSTAL_API_KEY` set; else controlled Graph bulk
