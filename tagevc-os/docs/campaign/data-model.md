# Data model

Tables prefixed `ecc_*` in `supabase/phase_email_campaign_platform.sql`.

Contact extensions: `email_permission` (default `opted_in`), `engagement_score`, `lifecycle`.

CRM identity remains `public.contacts` + org links.
