/**
 * Re-export platform Cards | List helpers.
 * Prefer importing from `@/lib/platform/view-mode` in new code.
 */
export {
  VIEW_MODE_DEFAULTS,
  VIEW_MODE_STORAGE_PREFIX,
  defaultViewModeFor,
  parseViewMode,
  viewModeStorageKey,
  type ViewMode,
  type ViewModeSurface,
} from '@/lib/platform/view-mode';
