/**
 * Canonical short path on card.tagevc.com: /p/{public_id}
 * Same handler as /card/p/{public_id} (app-host fallback).
 */
export { default, generateMetadata } from '@/app/card/p/[public_id]/page';
