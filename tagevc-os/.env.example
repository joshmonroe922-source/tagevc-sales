# Supabase (required in production)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_full_not_truncated

# Optional: server-side writes (webhooks, background persist). Prefer this in production.
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# App URL (OAuth redirects)
# NEXT_PUBLIC_APP_URL=https://app.tagevc.com

# Phase 9: prefer normalized os_* tables even when empty (read cutover / empty-snapshot drills)
# USE_NORMALIZED_TABLES=1

# Phase 15–16: snapshot write cutover (reversible; default = dual-write ON)
# WRITE_CUTOVER_MATURE=1
# WRITE_CUTOVER_ALL=1
# SNAPSHOT_SKIP_DOMAINS=deal_flow,tickets,documents,ma,re
# WRITE_SNAPSHOTS=0
# SNAPSHOT_WRITE_DOMAINS=ma,re

# Phase 16: Sentry (optional)
# SENTRY_DSN=https://…@o….ingest.sentry.io/…
# SENTRY_TRACES_SAMPLE_RATE=0.05

# Phase 13: email digests (optional)
# RESEND_API_KEY=re_xxx
# DIGEST_FROM_EMAIL=noreply@tagevc.com
# DIGEST_SECRET=long_random_shared_with_cron
# CRON_SECRET=same_or_separate

# Think Tank (Grok / xAI) — server-side only; never expose to browser
# XAI_API_KEY=

# Phase 64 — cross-portal presence (Available / DND) + soft message alerts
# TAGE_PRESENCE_SECRET=
# MS Graph calendar presence (optional; fail soft without these)
# MS_GRAPH_TENANT_ID=
# MS_GRAPH_CLIENT_ID=
# MS_GRAPH_CLIENT_SECRET=
# Requires app permission Calendars.Read (+ MailboxSettings.Read) for calendar busy/focus
# GROK_API_KEY=          # alias accepted
# XAI_MODEL=grok-3-mini
# XAI_BASE_URL=https://api.x.ai/v1

# Phase 14–16: protect admin APIs (DIGEST_SECRET / CRON_SECRET) or use admin session

# DocuSign JWT + Connect — full Josh checklist: docs/DOCUSIGN_JOSH_CHECKLIST.md
# Without these, sends stay mock (ENV-… ids). Never commit private key / HMAC.
# DOCUSIGN_INTEGRATION_KEY=
# DOCUSIGN_USER_ID=
# DOCUSIGN_PRIVATE_KEY=          # PEM; \n ok in Vercel
# DOCUSIGN_ACCOUNT_ID=           # ENT-FIRM default / fallback
# DOCUSIGN_ACCOUNT_ID_FIRM=      # Tage Venture Capital
# DOCUSIGN_ACCOUNT_ID_R619=      # Recruit 619
# DOCUSIGN_ACCOUNT_ID_SIGNENT=   # Signent HR
# DOCUSIGN_ACCOUNT_ID_INDA=      # Instant NDA
# DOCUSIGN_OAUTH_HOST=account.docusign.com          # demo: account-d.docusign.com
# DOCUSIGN_BASE_PATH=https://na4.docusign.net       # copy Account Base URI; demo: https://demo.docusign.net
# DOCUSIGN_CONNECT_HMAC_SECRET=  # Connect Include HMAC → X-DocuSign-Signature-1
# DOCUSIGN_WEBHOOK_SECRET=       # optional header x-tagevc-webhook-secret
# Connect URL (DocuSign Admin): https://app.tagevc.com/api/docusign/webhook

# AP inbound invoice parse (D05) — Josh creates entity mailboxes; we only verify this secret
# AP_INBOUND_WEBHOOK_SECRET=

# Phase 21–24 Marketing
# MARKETING_AI_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# MARKETING_SCHEDULER_ENABLED=1
# MARKETING_TOKEN_SECRET=at_least_16_chars_for_aes
# D11=C — marketing stubs banned; these flags are ignored / fail closed
# MARKETING_FORCE_STUB_PUBLISH=0
# MARKETING_ALLOW_STUB_OAUTH=0
# MARKETING_ALLOW_STUB_PUBLISH=0
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# LINKEDIN_ADS_API=0
# LINKEDIN_ADS_ACCESS_TOKEN=
# LINKEDIN_API_VERSION=202607
# LINKEDIN_ADS_CURRENCY=USD
# META_ADS_CURRENCY=USD
# X_CLIENT_ID=
# X_CLIENT_SECRET=
# META_APP_ID=
# META_APP_SECRET=
# META_API_VERSION=v25.0
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=
# TIKTOK_DEFAULT_VIDEO_URL=
# TIKTOK_PUBLISH_DIRECT=0
# Blog / CMS publish (POST JSON to site ingest). LIVE when set.
# BLOG_PUBLISH_WEBHOOK_URL=https://example.com/api/marketing/blog-ingest
# BLOG_PUBLISH_WEBHOOK_SECRET=

# Stage 4d archive retention confirm (ISO date)
# ARCHIVE_EXPORT_CONFIRMED_AT=2026-07-20T00:00:00.000Z

# Phase 25 Stage 4e DROP approval (never auto-drops from app)
# SNAPSHOT_DROP_APPROVED_AT=2026-10-20T00:00:00.000Z
# SNAPSHOT_DROP_APPROVED_BY=ops@tagevc.com
# ALLOW_SNAPSHOT_DROP=0
# SNAPSHOT_SOFT_RENAMED_AT=
# SNAPSHOT_RETIRED_TABLE_NAME=
# SNAPSHOT_SOFT_RENAME_APPROVED_AT=
# SNAPSHOT_SOFT_RENAME_APPROVED_BY=
# SNAPSHOT_SOFT_RENAME_SOAK_DAYS=7

# Phase 40–41 snapshot export signing / external retention (server-held)
# SNAPSHOT_EXPORT_HMAC_KEY_ID=snapshot-export-2026-01
# SNAPSHOT_EXPORT_HMAC_KEY=base64_32+_bytes
# SNAPSHOT_EXPORT_HMAC_KEYS={"snapshot-export-2026-01":"base64_32+_bytes"}
# SNAPSHOT_RETENTION_ALLOWED_HOSTS=evidence.example.com
# SNAPSHOT_RETENTION_DESTINATIONS={"archive_primary":{"url":"https://evidence.example.com/object.json","hash_header":"x-evidence-sha256"}}

# Phase 41 externally verifiable ed25519 receipts (PKCS8/SPKI DER base64)
# Publish only PUBLIC keys for offline verify; keep private keys server-side.
# SNAPSHOT_EXPORT_ED25519_KEY_ID=snapshot-ed25519-2026-01
# SNAPSHOT_EXPORT_ED25519_PRIVATE_KEY=base64_pkcs8_der
# SNAPSHOT_EXPORT_ED25519_PUBLIC_KEY=base64_spki_der
# SNAPSHOT_EXPORT_ED25519_PRIVATE_KEYS={"snapshot-ed25519-2026-01":"base64_pkcs8_der"}
# SNAPSHOT_EXPORT_ED25519_PUBLIC_KEYS={"snapshot-ed25519-2026-01":"base64_spki_der"}

# Phase 41 SLO simulation export HMAC (metadata digests only)
# SLO_SIMULATION_EXPORT_HMAC_KEY_ID=slo-sim-export-2026-01
# SLO_SIMULATION_EXPORT_HMAC_KEY=base64_32+_bytes
# SLO_SIMULATION_EXPORT_HMAC_KEYS={"slo-sim-export-2026-01":"base64_32+_bytes"}
# SLO_SIMULATION_EXPORT_RETENTION_DAYS=90

# Phase 42–43 snapshot cold HEAD cadence (hours; default 168)
# Reused for firm-wide verify publish + production cold HEAD schedules.
# SNAPSHOT_COLD_RETENTION_CHECK_CADENCE_HOURS=168
# Production cold HEAD requires SNAPSHOT_RETENTION_DESTINATIONS + ALLOWED_HOSTS above.

# Phase 43 marketing SLO critical ops alerts (reuse SLO webhook allowlist)
# SLO_WEBHOOK_OPS_ALERTS=https://hooks.example.com/slo
# SLO_WEBHOOK_ALLOWED_HOSTS=hooks.example.com
# MARKETING_SLO_ALERT_COOLDOWN_HOURS=24
# MARKETING_CONFLICT_AGING_DAYS=7

# Phase 44 ops alert cooldowns + snapshot canary cadence (reuse SLO webhook allowlist)
# DOCUSIGN_ARCHIVE_ALERT_COOLDOWN_HOURS=24
# INTUNE_OPS_ALERT_COOLDOWN_HOURS=24
# SNAPSHOT_PHASE44_CANARY_CADENCE_HOURS=6
# SNAPSHOT_RETENTION_ALERT_COOLDOWN_HOURS=24

# Phase 45 snapshot consecutive failure paging threshold (default 3)
# SNAPSHOT_CONSECUTIVE_FAILURE_THRESHOLD=3

# Phase 46 snapshot on-call paging (optional; allowlisted via SLO_WEBHOOK_ALLOWED_HOSTS)
# SNAPSHOT_ONCALL_WEBHOOK=https://hooks.example.com/oncall

# Phase 47 on-call acknowledgment SLO (minutes; default 60)
# SNAPSHOT_ONCALL_ACK_SLO_MINUTES=60

# Phase 48 allowlisted owner digest webhooks (JSON map destination_key→HTTPS URL;
# hosts must also appear in SLO_WEBHOOK_ALLOWED_HOSTS). Not full push.
# SLO_OWNER_DIGEST_WEBHOOKS={"owner_digest":"https://hooks.example.com/digest"}

# Phase 48 CI-integrated offline_script dual acceptance for snapshot cutover
# SNAPSHOT_CI_CUTOVER_ENABLED=1

# Phase 49: comma-separated branches that always require CI offline_script
# dual acceptance before ed25519 cutover completes (default main,production;
# the database policy table is the actual enforced source of truth).
# SNAPSHOT_CI_PROTECTED_BRANCH_REQUIRED=main,production

# Phase 50: allowlisted webhook for paging protected_branch_cutover_blocked
# alerts (optional; best-effort — delivery outcome is always recorded via
# page_snapshot_protected_branch_cutover_blocked_phase50, never silently
# dropped). Host must also appear in SLO_WEBHOOK_ALLOWED_HOSTS.
# SNAPSHOT_PHASE50_PAGE_WEBHOOK_URL=https://hooks.example.com/snapshot-oncall
# SNAPSHOT_PHASE50_PAGE_DESTINATION_KEY=oncall

# Phase 50 CI: node scripts/ci-snapshot-phase50-path-guard.mjs --check
# enforces the Phase 48/49 CI offline_script dual-acceptance --check gate
# on PRs touching cutover-adjacent paths. Optionally reuse a prior
# ci-snapshot-cutover-accept.mjs --check exit code in the same job:
# SNAPSHOT_CI_CHECK_EXIT_CODE=0

# Phase 51: number of consecutive healthy Phase 50 audit-export windows a
# soaked/auto-reject marketing cohort must have before the ops tick will
# auto-PROPOSE (never auto-approve) a dry-run→promote promotion. Money still
# requires two distinct human approvers via the existing Phase 50 path.
# MARKETING_PHASE51_AUTO_PROPOSE_WINDOWS=3

# Phase 51: days a DocuSign second-approver reminder can go unanswered before
# a third-approver escalation receipt is raised (notification-only — never
# auto-activates a budget or creates/voids/resends envelopes).
# DOCUSIGN_PHASE51_ESCALATION_THRESHOLD_DAYS=3

# GitHub Actions: .github/workflows/snapshot-path-guard.yml runs the Phase 50
# path-guard script as a PR check named `ci-snapshot-phase50-path-guard`. Mark
# it required under Settings → Branches → Branch protection rules → Require
# status checks to pass. Phase 51 records read-only evidence of whether it is
# actually configured as required via record_phase51_required_check_verification
# (never mutates branch protection itself).

# Phase 52: read-only GitHub branch-protection verify script
# node scripts/ci-snapshot-phase52-branch-protection-verify.mjs
# GITHUB_TOKEN=... or GH_TOKEN=... (read-only repo scope for GET only)
# GITHUB_REPOSITORY=owner/repo  (or GITHUB_OWNER + GITHUB_REPO)
# SNAPSHOT_PHASE52_PROTECTED_BRANCH=main
# SNAPSHOT_PHASE52_CHECK_CONTEXT=ci-snapshot-phase50-path-guard
# SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=...  (optional evidence recording)
# Making ci-snapshot-phase50-path-guard required is still a human admin action:
# Settings → Branches → Branch protection → Require status checks → add the check.

# Phase 52: pending-proposals digest backlog threshold before raising an ops alert.
# MARKETING_PHASE52_PENDING_DIGEST_BACKLOG_THRESHOLD=5

# Phase 52: days a third-approver DocuSign reminder can go unanswered before a
# fourth-approver escalation receipt is raised (notification-only).
# DOCUSIGN_PHASE52_FOURTH_ESCALATION_THRESHOLD_DAYS=3

# Phase 25 MDM offboarding webhook (optional)
# MDM_WEBHOOK_URL=https://mdm.example.com/hooks/offboard
# MDM_WEBHOOK_SECRET=
# Intune retirement has no auto-retire switch; every action requires approval.
# IT_OFFBOARD_MAILBOX_MODE=disable_only
# IT_OFFBOARD_LITIGATION_HOLD=0
# EXCHANGE_AUTOMATION_URL=
# EXCHANGE_AUTOMATION_SECRET=
# Exchange automation must return {"verified":true,"job_id":"..."} only after
# provider-side litigation-hold verification; accepted jobs remain pending.
# MS_GRAPH_SOFT_DELETE_USER=0

# Local only — never enable on Vercel production
# DEV_BYPASS_AUTH=1

# Multi-subsidiary readiness (P1–P6): subsidiary ticket API auth
# Shared secret for HMAC Bearer tokens / header fallback. Prefer per-client.
# SUBSIDIARY_API_SECRET=long_random_shared_with_subsidiary_portals
# SUBSIDIARY_API_SECRET_RECRUIT619_PORTAL=
# SUBSIDIARY_API_SECRET_INSTANTNDA_PORTAL=
# SUBSIDIARY_API_SECRET_SIGNENT_HR_PORTAL=
# Register future portal clients without code change (comma client:entity pairs):
# SUBSIDIARY_API_CLIENTS=acme_portal:ENT-ACME
# Cross-portal presence (Bearer on /api/presence) — share with each portal:
# TAGE_PRESENCE_SECRET=

# Recruit inbound (Phase 55 intake — separate from multi-sub track)
# TAGE_SS_WEBHOOK_SECRET=shared_with_recruit
# TAGE_ROLLUP_SECRET=shared_with_recruit
# RECRUIT_PORTAL_URL=https://portal.recruit619.com
# RECRUIT_SS_INTAKE_SECRET=

# Phase 80 — Verified First screening spine (fail-closed unless LIVE=1)
# VERIFIED_FIRST_LIVE=0
# VERIFIED_FIRST_API_KEY=
# VERIFIED_FIRST_WEBHOOK_SECRET=
# VERIFIED_FIRST_API_BASE=https://api.verifiedfirst.com/v1
# Webhook: POST /api/screening/verified-first/webhook

# Phase 89 — Partner spine (see docs/PARTNER_SPINE.md). Never commit real secrets.
# Apply SQL: supabase/phase89_partner_spine.sql
# TAGE_PARTNER_PROVISION_SECRET=  # Bearer for POST /api/partners/provision-entity
#
# Dialpad (phone + SMS + AI)
# DIALPAD_API_KEY=
# DIALPAD_WEBHOOK_SECRET=
# DIALPAD_LIVE=0
#
# MyBasePay EOR (Recruit 619 first)
# MYBASEPAY_API_KEY=
# MYBASEPAY_WEBHOOK_SECRET=
# MYBASEPAY_API_BASE=
# MYBASEPAY_LIVE=0
#
# Apollo (contacts → unified DB)
# APOLLO_API_KEY=
# APOLLO_LIVE=0
#
# Gusto payroll + commission push (invoice paid → payroll)
# GUSTO_API_TOKEN=
# GUSTO_COMPANY_UUID=
# GUSTO_WEBHOOK_SECRET=
# GUSTO_LIVE=0
#
# LinkedIn Recruiter (two-way sync — account coming)
# LINKEDIN_RECRUITER_CLIENT_ID=
# LINKEDIN_RECRUITER_CLIENT_SECRET=
# LINKEDIN_RECRUITER_LIVE=0
#
# Appcast (job publish — R619 portal also uses APPCAST_*)
# APPCAST_API_KEY=
# APPCAST_EMPLOYER_ID=
# APPCAST_WEBHOOK_SECRET=
# APPCAST_LIVE=0
#
# Marketing presence — Google Business / GA4 / LinkedIn Company (Marketing SS)
# GOOGLE_BUSINESS_CLIENT_ID=
# GOOGLE_BUSINESS_CLIENT_SECRET=
# GOOGLE_BUSINESS_REFRESH_TOKEN=
# GOOGLE_BUSINESS_LIVE=0
# GA4_PROPERTY_ID=
# GA4_SERVICE_ACCOUNT_JSON=
# GA4_LIVE=0
# LINKEDIN_COMPANY_CLIENT_ID=
# LINKEDIN_COMPANY_CLIENT_SECRET=
# LINKEDIN_COMPANY_ORGANIZATION_URN=
# LINKEDIN_COMPANY_LIVE=0
# Webhooks: POST /api/partners/{dialpad|mybasepay|gusto|appcast}/webhook

# Phase 89 — Partner spine (see docs/PARTNER_SPINE.md). Never invent values.
# Apply SQL: supabase/phase89_partner_spine.sql
# DIALPAD_LIVE=0
# DIALPAD_API_KEY=
# DIALPAD_WEBHOOK_SECRET=
# MYBASEPAY_LIVE=0
# MYBASEPAY_API_KEY=
# MYBASEPAY_WEBHOOK_SECRET=
# APOLLO_LIVE=0
# APOLLO_API_KEY=
# GUSTO_LIVE=0
# GUSTO_API_TOKEN=
# GUSTO_COMPANY_UUID=
# LINKEDIN_RECRUITER_LIVE=0
# LINKEDIN_RECRUITER_CLIENT_ID=
# LINKEDIN_RECRUITER_CLIENT_SECRET=
# LINKEDIN_RECRUITER_ACCESS_TOKEN=
# APPCAST_LIVE=0
# APPCAST_API_KEY=
# APPCAST_EMPLOYER_ID=
# APPCAST_WEBHOOK_SECRET=
# Marketing presence (Marketing Shared Services — all entities)
# GOOGLE_BUSINESS_LIVE=0
# GOOGLE_BUSINESS_CLIENT_ID=
# GOOGLE_BUSINESS_CLIENT_SECRET=
# GOOGLE_BUSINESS_REFRESH_TOKEN=
# GA4_LIVE=0
# GA4_PROPERTY_ID=
# GOOGLE_ANALYTICS_CLIENT_ID=
# GOOGLE_ANALYTICS_CLIENT_SECRET=
# GOOGLE_ANALYTICS_REFRESH_TOKEN=
# LINKEDIN_COMPANY_LIVE=0
# LINKEDIN_COMPANY_CLIENT_ID=
# LINKEDIN_COMPANY_CLIENT_SECRET=
# LINKEDIN_COMPANY_ACCESS_TOKEN=

# Phase 70/81 — Intuit Enterprise Suite (sole GL; server-side only)
# IES_CLIENT_ID=
# IES_CLIENT_SECRET=
# IES_TOKEN_SECRET=at_least_16_chars
# IES_ENVIRONMENT=sandbox
# IES_REDIRECT_URI=https://app.tagevc.com/api/finance/ies/oauth/callback
# Exact opt-ins. Both default off; writes always require two human approvals.
# IES_SYNC_ENABLED=0
# IES_WRITE_ENABLED=0

# Platform email (Graph + analytics) — see docs/PLATFORM_EMAIL.md + docs/M365_MAIL_SETUP.md
# Apply SQL: supabase/phase_platform_email.sql
# App: Tage VC OS — Application (client) ID below (reuse; do not create a second app)
# AZURE_TENANT_ID=aecc0efa-a429-4b4f-8c77-c2957b8263ab
# AZURE_CLIENT_ID=905649ff-1aee-4683-87e0-5d6d2005aea5
# AZURE_CLIENT_SECRET=   # app already has 3 secrets — paste known value in Vercel only; create new if lost
# MS_GRAPH_TENANT_ID=aecc0efa-a429-4b4f-8c77-c2957b8263ab
# MS_GRAPH_CLIENT_ID=905649ff-1aee-4683-87e0-5d6d2005aea5
# MS_GRAPH_CLIENT_SECRET=
# MS_GRAPH_SCOPES=openid offline_access User.Read Mail.ReadWrite Mail.Send
# MS_GRAPH_REDIRECT_URI=https://app.tagevc.com/api/microsoft/callback
# Bootstrap aliases on joshmonroe@tagevc.com (send-as + inbound To: routing)
# M365_HOST_MAILBOX=joshmonroe@tagevc.com
# M365_ALIAS_AP=accountspayable@tagevc.com
# M365_ALIAS_AR=accountsreceivable@tagevc.com
# M365_ALIAS_W9=w-9@tagevc.com
# RESEND_FROM_EMAIL=Tage Venture Capital <hello@tagevc.com>
# RESEND_WEBHOOK_SECRET=whsec_…   # Supabase edge resend-webhook
# PLATFORM_EMAIL_TRACKING_SECRET=  # optional HMAC for tracking tokens
# My Recruiting Desk origin (ENT-R619 day-to-day UX)
# NEXT_PUBLIC_MY_RECRUITING_DESK_URL=https://app.recruit619.com
# NEXT_PUBLIC_RECRUIT_PORTAL_URL=https://portal.recruit619.com

